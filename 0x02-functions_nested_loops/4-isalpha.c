#include "main.h"
/**
 * _isalpha - Checks whether character is a letter (lowercase or uppercase)
 * @c: Character to be checked
 * Return: 1 if true,
 * if not, 0
 */
int _isalpha(int c)
{
	int i = _isalpha(c);

	if (i > 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
