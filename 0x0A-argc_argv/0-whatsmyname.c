#include <stdio.h>
/**
  * main - Entry point
  * @argc: Argument count
  * @argv: Arguments
  *
  * Return: 0
  */
int main(int argc __attribute__((unused)), char *argv[])
{ /* prints the name of the file */
	printf("%s\n", argv[0]);

	return (0);
}
