#include <stdio.h>

/**
  * main - causes infinte loop
  * Return: 0
  */

int maind(void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	i = 0;
	
	/**while (I < 10)
	  *{
	  *	putchar(i);
	  *}
	  */
	printf("Infinte loop avoid! \\o/\n");

	return (0);
}
